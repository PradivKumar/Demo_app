<%provide(:one, "is")%>
<%provide(:many, "are")%>
<%provide(:msg, "celebrating Birthday!")%>

	<%if @users.count == 1%>
		<%=u.name%><%=yield(:one)%> 
		<%=yield(:msg)%> 
	<%else%>
		<%@users.each do |u|%>
			<%= u.name %>,  
		<%#= u.dob %>
		<%end%>
		<%=yield(:many)%> 
		<%=yield(:msg)%>
<%end%>
		<br><%= link_to "Send wish!", '#'%>
